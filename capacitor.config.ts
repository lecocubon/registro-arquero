import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.lecocubon.arquero',
  appName: 'Arquero',
  webDir: 'dist',
  android: {
    backgroundColor: '#101310',
  },
};

export default config;
